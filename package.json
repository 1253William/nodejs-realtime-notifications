{
  "name": "nodejs-realtime-notifications",
  "version": "1.0.0",
  "description": "A basic server using Node.js, Express, and the ws WebSocket library. Clients can connect to the server via WebSockets to receive real-time notifications. Notifications can be triggered by sending HTTP POST requests to the server, making it easy to test using Postman.",
  "main": "./src/server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.9",
    "socket.io": "^4.8.1",
    "ws": "^8.18.1"
  }
}
